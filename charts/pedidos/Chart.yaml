apiVersion: v2
name: pedidos
description: Sistema de gestión de pedidos (backend FastAPI + PostgreSQL)
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: db
    version: 0.1.0
    repository: file://subcharts/db
    condition: db.enabled
  - name: backend
    version: 0.1.0
    repository: file://subcharts/backend
    condition: backend.enabled
